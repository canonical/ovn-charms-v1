variables:
  openstack-origin: &openstack-origin cloud:focal-xena

local_overlay_enabled: False

series: focal

applications:

  vault:
    charm: ch:vault
    num_units: 1
    # The vault charm raft storage support appeared in the Jammy charm
    series: jammy
    channel: latest/edge

  ovn-central:
    charm: ../../../ovn-central.charm
    num_units: 3
    options:
      source: *openstack-origin

  nrpe:
    charm: cs:nrpe

relations:

  - - 'ovn-central:certificates'
    - 'vault:certificates'
  - - 'nrpe:nrpe-external-master'
    - 'ovn-central:nrpe-external-master'
